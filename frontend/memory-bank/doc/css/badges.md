# Badges

`.badge` + `.badge-{color}`: primary (purple), success (green), danger (red), warning (orange), info (cyan), blue, blue-300

Padding 1px 10px, border-radius 25px, font-size --fs-300, white text, 300ms transition.
