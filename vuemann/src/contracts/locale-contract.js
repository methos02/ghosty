/**
 * Locale contract
 * @typedef {Object} LocaleService
 * @property {(textKey: string, params?: Record<string, string | number>) => string} t
 * @property {() => string} getCurrentLocale
 */
export {}
